<div class="container mb-5">
  <%= turbo_stream_from dom_id(current_user, :matches) %>
  <h1>My Matches</h1>
  <ul><% @matches1.each do |match| %>
  <li> See <% match.matcher.dogs.each do |dog| %>
  <%= dog.name %> (🐕)
  <% end %>
  & <%= match.matcher.first_name %>'s (🧍) <%= link_to "profile", show_profile_path(match.matcher) %> or have a <%= link_to "chat", match_path(match) %> 💬</li>

  <% end %>
  <% @matches2.each do |match| %>
  <li> See <% match.matchee.dogs.each do |dog| %>
  <%= dog.name %> (🐕)
  <% end %>
  & <%= match.matchee.first_name %>'s (🧍) <%= link_to "profile", show_profile_path(match.matchee) %> or have a <%= link_to "chat", match_path(match) %> 💬</li>

  <% end %>
  </ul>
</div>
<div id="<%= dom_id(current_user, :matches) %>"></div>
